[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lightstim"
version = "0.1.0"
description = "QEC simulation framework built on Stim"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [
    {name = "Xiang Fang"}
]
classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Physics",
]

dependencies = [
    "numpy>=1.20.0",
    "stim>=1.15.0",
    "sinter>=1.15.0",
    "PyMatching>=2.0.0",
    "pandas",
    "scipy>=1.9.0",
]

[project.optional-dependencies]
plot = [
    "matplotlib",
    "seaborn",
]
server = [
    "fastapi>=0.110.0",
    "uvicorn>=0.29.0",
]
decoders = [
    "stimbposd",                # BP+OSD
    "mwpf",                     # MWPF
    "frozendict",
    "frozenlist",
    "relay-bp[stim]",           # Relay-BP
    # Tesseract (beam-search MLE). A prebuilt wheel may not match every CPU;
    # build it from source if it fails to import.
    "tesseract-decoder",
    "ldpc",                     # Plain BP (ldpc-bp)
    # The exact-MLE decoder (mle-ilp) uses the core scipy dependency.
]
gpu = [
    "cudaq-qec",
]
deq = [
    # Microsoft's qdk-ec toolkit (github.com/microsoft/qdk-ec) — real,
    # normally-installable PyPI packages. `deqagram` ships a prebuilt wheel,
    # so no Rust toolchain is needed just to validate exported .deq text.
    "deq>=0.4",
    "deqagram>=0.4",
]
dev = [
    "pytest",
    "jupyter",
    "ipykernel",
    "matplotlib",
    "seaborn",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["lightstim*"]

[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "gpu: marks tests requiring a CUDA device",
]
